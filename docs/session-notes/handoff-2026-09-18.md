# The Palms handoff, 2026-09-18 (Friday)

Read this first, then `docs/next-actions.md`. Written for a session that has
never seen this one. This repo is public, so business detail and anything
private live in the operator's workspace memory, not here.

## What just happened

First session since 2026-06-23 (nearly three months with no commits from
anyone). Winston asked for a full review and refresh ahead of a meeting
between the developer and Farley Creative. Everything below was verified
against production, not the local checkout.

Shipped and deploy-confirmed on Vercel (production READY):

| Commit | What |
|---|---|
| `e1e85d4` | Next 16.2.9 → 16.3.5, nodemailer 9.0.1 → 9.1.1 (npm audit 8 advisories incl. 1 critical → 0). Daily backup now covers `users` + `brokerages`. |
| `86fc77f` | Em-dash pass on public copy, metadata, alt text, share-card text, broadcast footer. Punctuation only. |
| `f6ee74b` | Repo docs rewritten to the live state (they still described the June-19 empty scaffold). |
| _this commit_ | Inventory empty state without the one-click "seed sample residences" button; this handoff. |

Hub data cleanup (production Blob, off-Vercel snapshot taken first at
`~/palms-backups/2026-09-18T15-56`):

- Removed our own June test artifacts: document `doc_2xvdcq4u` ("E2E size test
  (auto-delete).bin") and its 6 MB blob, draft broadcast `b_t37351bl`
  ("New release"), campaign `cmp_mk8388q0` ("Phase 1 broker push").
- Removed the 8 seeded sample residences (all `sample: true`). They put
  "$10M of inventory / $3.1M sold" on the owner's dashboard and contradicted
  the real facts (2,900 sq ft vs 5,000–10,000).
- The only lead (`pal_r5724h25`, a vendor sales pitch from 06-22) moved to
  Lost with an activity note. Zero real Founders' List signups to date.
- Created the developer's operator login (open since June). Verified on prod:
  correct password 200, wrong 401, header shows her email.

## Found and NOT fixable from here: thepalms.dev email is down

- Google's MX answers `550 5.1.1 account does not exist` for hello@, the team's
  mailboxes and a random address (controls on other Workspace domains answer
  250, so the probe is sound). On 2026-09-01 the domain still accepted mail.
- SMTP sign-in for `hello@` returns `534 5.7.9 WebLoginRequired`; IMAP refuses.
- Effect: Founders' List signups still save to the hub, but the email alert
  fails silently; the public footer's `info@thepalms.dev` bounces; broker
  replies to broadcasts would bounce.
- Most likely a suspended Workspace subscription (trial or billing). Google's
  notices went to hello@ or the signup contact, not haveebot.
- **Fix = Winston signs in at admin.google.com as `hello@thepalms.dev`.** Then
  re-verify: RCPT probe → 250; SMTP auth OK; if the app password died, set a
  new one as `WORKSPACE_SMTP_PASSWORD` via the Vercel API; send one test alert.

## State of everything else (verified 2026-09-18)

- Public site 200, www → apex 308, noindexed (robots Disallow), 0 console errors.
- Hub: `thepalms.dev` → login; every `/api/hub/*` 401 without a session.
- Backups: 91 consecutive daily snapshots, no gaps; manual run today = 571 records.
- Domains: both auto-renew (2027-06-19); TLS auto-renewing; DNS (MX, SPF,
  DKIM, DMARC) published on Vercel DNS.
- Vercel env: no `BROADCAST_MAILING_ADDRESS`, no Google OAuth (both still open).
- Data: 560 contacts (362 emailable, 494 with phone), 7 documents, 0 units,
  1 lead (Lost), 2 users.
- The proxy-bypass advisory (GHSA-6gpp-xcg3-4w24) needed a single-locale i18n
  config, which this app doesn't have, so the hub was not exposed before the patch.
- The Founders' List form HAS a honeypot enforced server-side (since day one).
  An earlier note saying it had no bot protection was wrong.

## Awaiting Winston

1. Restore Workspace email (above).
2. The CAN-SPAM postal address for `BROADCAST_MAILING_ADDRESS`.
3. Hand Shana her hub login (temp password was given in chat, not stored
   anywhere); say if you want it swapped for one you choose.
4. Decide on the pre-launch storage hardening (private Blob for leads/users)
   before any traffic is driven to the Founders' List.

## Awaiting the developer and Farley Creative

Homesite data for Inventory (the 21 lots, pricing, phases), renderings / site
plan (for the page and the 21-lot map), the final brand kit, a launch date,
and the public contact address.

## Pickup

```bash
cd ~/Projects/workspace/the-palms-at-island-moorings && git fetch -q && git rev-list --count HEAD..origin/main
node scripts/backup-collections.mjs          # off-Vercel snapshot + per-collection counts
curl -sI https://thepalmsatislandmoorings.com/ | head -1
curl -sI https://thepalms.dev/ | head -1
```
